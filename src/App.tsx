import Sidebar from "./components/Sidebar";

const App = () => {
  return (
    <div className="w-full h-screen bg-[#EFF5FF] flex flex-col md:justify-center md:items-center">
      <Sidebar />
    </div>
  );
};

export default App;
